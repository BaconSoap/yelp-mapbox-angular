## yelp-mapbox-angular

experimenting with mapbox by using the yelp api


```
npm install
bower install
grunt watch
```

### Mapbox Credentials

make credentials.js have:

```js
var credentials = {
	mapbox: {
		mapKey: 'account.key'
	}	
};
```

